PortSystem          1.0
PortGroup           golang 1.0

go.setup            github.com/bkneis/lazyaws 0.1.0 v
go.offline_build    yes
github.tarball_from archive

name                lazyaws
categories          sysutils
platforms           darwin
license             MIT
maintainers         {github.com/bkneis @bkneis} openmaintainer

description         Terminal UI for browsing AWS resources
long_description    {*}${description}. Navigate S3, Lambda, DynamoDB, EC2, \
                    and more from your terminal, inspired by lazygit.

checksums           rmd160  REPLACE \
                    sha256  REPLACE \
                    size    REPLACE

# go.vendors block — generate with:
#   go install github.com/amake/go2port@latest
#   go2port go.sum
go.vendors          REPLACE_WITH_GO2PORT_OUTPUT

build.args          -ldflags "-s -w -X main.version=${version}"

destroot {
    xinstall -m 755 ${worksrcpath}/lazyaws ${destroot}${prefix}/bin/lazyaws
}
